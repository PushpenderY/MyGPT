// Convenience root entry point. The real app bootstrapping lives in src/index.js
// (env config -> MongoDB connection -> app.listen). This file exists so the
// project can be started with `node server.js` from the backend root, matching
// a typical MERN project layout.
import "./src/index.js";
